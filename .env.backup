# =============================================================================
# AI Chat Platform - Environment Configuration
# AI Chat Platform - Ortam Yapılandırması
# =============================================================================
# 
# This file contains all environment variables for the AI Chat Platform monorepo.
# Bu dosya AI Chat Platform monorepo'su için tüm ortam değişkenlerini içerir.
#
# IMPORTANT SECURITY NOTES:
# ÖNEMLİ GÜVENLİK NOTLARI:
# - Never commit this file to version control
#   - Bu dosyayı versiyon kontrolüne asla commit etmeyin
# - Change all secrets in production environments
#   - Üretim ortamlarında tüm gizli anahtarları değiştirin
# - Use strong, randomly generated secrets (min 32 characters for JWT_SECRET)
#   - Güçlü, rastgele oluşturulmuş gizli anahtarlar kullanın (JWT_SECRET için min 32 karakter)
#
# =============================================================================

# -----------------------------------------------------------------------------
# Core Application Settings / Temel Uygulama Ayarları
# -----------------------------------------------------------------------------

# Application environment (development, production, test)
# Uygulama ortamı (development, production, test)
NODE_ENV=development

# Logging level (trace, debug, info, warn, error, fatal)
# Günlükleme seviyesi (trace, debug, info, warn, error, fatal)
LOG_LEVEL=info

# API Gateway host address
# API Gateway host adresi
# Use 0.0.0.0 to bind to all interfaces
# Tüm arayüzlere bağlanmak için 0.0.0.0 kullanın
API_HOST=0.0.0.0

# API Gateway port
# API Gateway portu
API_PORT=4000  # No change - unique port

# Web application port
# Web uygulaması portu
WEB_PORT=3000  # No change - unique port

# Skip dotenv loading (set to true if loading env vars from another source)
# Dotenv yüklemeyi atla (ortam değişkenleri başka bir kaynaktan yükleniyorsa true olarak ayarlayın)
SKIP_DOTENV=false

# -----------------------------------------------------------------------------
# Database Configuration / Veritabanı Yapılandırması
# -----------------------------------------------------------------------------

# PostgreSQL database connection string
# PostgreSQL veritabanı bağlantı dizisi
# Format: postgresql://[user]:[password]@[host]:[port]/[database]?schema=public
# Format: postgresql://[kullanıcı]:[şifre]@[host]:[port]/[veritabanı]?schema=public
# 
# For local development with Docker Compose:
# Docker Compose ile yerel geliştirme için:
#   postgresql://ai_chat_user:ai_chat_password@db:5432/ai_chat_db?schema=public
#
# For local development without Docker:
# Docker olmadan yerel geliştirme için:
#   postgresql://ai_chat_user:ai_chat_password@localhost:5436/ai_chat_db?schema=public
#
# For production, use a secure connection string with SSL:
# Üretim için, SSL ile güvenli bağlantı dizisi kullanın:
#   postgresql://user:password@host:5432/db?sslmode=require
#
# Required: Yes / Gerekli: Evet
DATABASE_URL=postgresql://ai_chat_user:wQHmmbmESr409P304s+eFMQWDEtUiGIA@localhost:5436/ai_chat_db?schema=public

# -----------------------------------------------------------------------------
# Redis Configuration / Redis Yapılandırması
# -----------------------------------------------------------------------------

# Redis connection URL
# Redis bağlantı URL'si
# Used for caching, rate limiting, and session storage
# Önbellekleme, hız sınırlama ve oturum depolama için kullanılır
# Format: redis://[password@][host]:[port]/[database]
# Format: redis://[şifre@][host]:[port]/[veritabanı]
#
# For local development with Docker Compose:
# Docker Compose ile yerel geliştirme için:
#   redis://redis:6379
#
# For local development without Docker:
# Docker olmadan yerel geliştirme için:
#   redis://localhost:6384
#
# For production with authentication:
# Kimlik doğrulama ile üretim için:
#   redis://:password@redis-host:6379/0
#
# Required: Yes / Gerekli: Evet
REDIS_URL=redis://localhost:6384  # No change - unique port

# -----------------------------------------------------------------------------
# Authentication & Security / Kimlik Doğrulama ve Güvenlik
# -----------------------------------------------------------------------------

# JWT signing secret (minimum 16 characters)
# JWT imzalama gizli anahtarı (minimum 16 karakter)
# MUST be changed in production - use a strong, randomly generated secret
# Üretimde MUTLAKA değiştirilmeli - güçlü, rastgele oluşturulmuş gizli anahtar kullanın
# Generate with: openssl rand -base64 32
# Oluşturma: openssl rand -base64 32
# Required: Yes / Gerekli: Evet
JWT_SECRET=lwvUPIv28VvMs1m6uE1mtex4/ehKEGgkGY2tSjzX/hE=

# JWT token expiration time
# JWT token süre sonu zamanı
# Format: number followed by unit (s, m, h, d, w)
# Format: birim ile sayı (s, m, h, d, w)
# Examples: 1h, 24h, 7d, 3600s
# Örnekler: 1h, 24h, 7d, 3600s
# Default: 1h / Varsayılan: 1h
JWT_EXPIRES_IN=1h

# Refresh token time-to-live in days
# Refresh token yaşam süresi (gün cinsinden)
# Default: 30 days / Varsayılan: 30 gün
REFRESH_TOKEN_TTL_DAYS=30

# JWT private key (RSA private key for RS256 algorithm)
# JWT özel anahtarı (RS256 algoritması için RSA özel anahtarı)
# Used for signing JWT tokens when using RS256
# RS256 kullanılırken JWT token'larını imzalamak için kullanılır
# Generate with: openssl genrsa -out private_key.pem 2048
# Oluşturma: openssl genrsa -out private_key.pem 2048
# Required: Yes (if using RS256) / Gerekli: Evet (RS256 kullanılıyorsa)
JWT_PRIVATE_KEY=-----BEGIN PRIVATE KEY-----

CjUb2f67Uyy64ID+oWb0jwBF
-----END PRIVATE KEY-----

# JWT public key (RSA public key for RS256 algorithm)
# JWT genel anahtarı (RS256 algoritması için RSA genel anahtarı)
# Used for verifying JWT tokens when using RS256
# RS256 kullanılırken JWT token'larını doğrulamak için kullanılır
# Generate with: openssl rsa -in private_key.pem -pubout -out public_key.pem
# Oluşturma: openssl rsa -in private_key.pem -pubout -out public_key.pem
# Required: Yes (if using RS256) / Gerekli: Evet (RS256 kullanılıyorsa)
JWT_PUBLIC_KEY=-----BEGIN PUBLIC KEY-----

zwIDAQAB
-----END PUBLIC KEY-----

# Session secret for secure session management
# Güvenli oturum yönetimi için oturum gizli anahtarı
# Used for signing session cookies
# Oturum çerezlerini imzalamak için kullanılır
# Generate with: openssl rand -base64 32
# Oluşturma: openssl rand -base64 32
SESSION_SECRET=i+6ddc/6dN/6D71RDDLQjUpSaqnAGd7T

# Encryption key for sensitive data encryption
# Hassas veri şifreleme için şifreleme anahtarı
# Used for encrypting sensitive data at rest
# Bekleyen hassas verileri şifrelemek için kullanılır
# Generate with: openssl rand -base64 32
# Oluşturma: openssl rand -base64 32
ENCRYPTION_KEY=etpqQjqYEOhiVwGaaxWoQzdKfZCXAoAg

# Bcrypt salt rounds for password hashing
# Şifre hash'leme için Bcrypt tuz turu
# Higher values are more secure but slower (10-12 recommended)
# Daha yüksek değerler daha güvenli ama daha yavaştır (10-12 önerilir)
BCRYPT_SALT_ROUNDS=10

# -----------------------------------------------------------------------------
# Superadmin Configuration / Süperadmin Yapılandırması
# -----------------------------------------------------------------------------

# Superadmin email address
# Süperadmin e-posta adresi
# Used for initial admin user creation
# İlk admin kullanıcı oluşturma için kullanılır
SUPERADMIN_EMAIL=admin@example.com

# Superadmin password
# Süperadmin şifresi
# MUST be changed in production
# Üretimde MUTLAKA değiştirilmeli
SUPERADMIN_PASSWORD=9VxQ1MMKuVvXZzxmEXt7GkG6mBK8qFrm

# Superadmin display name
# Süperadmin görünen adı
SUPERADMIN_NAME=Admin User

# -----------------------------------------------------------------------------
# Ollama Configuration / Ollama Yapılandırması
# -----------------------------------------------------------------------------

# Ollama service base URL
# Ollama servisi temel URL'si
# Used for local LLM inference
# Yerel LLM çıkarımı için kullanılır
# For local development:
# Yerel geliştirme için:
#   http://localhost:11434
#
# For Docker Compose:
# Docker Compose için:
#   http://ollama:11434
OLLAMA_BASE_URL=http://localhost:11435  # No change - unique port

# Default model to use when no model is specified
# Model belirtilmediğinde kullanılacak varsayılan model
DEFAULT_MODEL=llama3.1

# -----------------------------------------------------------------------------
# SMTP Email Configuration / SMTP E-posta Yapılandırması
# -----------------------------------------------------------------------------

# SMTP server host
# SMTP sunucu host'u
# Optional: If not provided, email service will be disabled
# Opsiyonel: Sağlanmazsa, e-posta servisi devre dışı bırakılacaktır
# Examples: smtp.gmail.com, smtp.sendgrid.net, smtp.mailgun.org
# Örnekler: smtp.gmail.com, smtp.sendgrid.net, smtp.mailgun.org
SMTP_HOST=

# SMTP server port
# SMTP sunucu portu
# Default: 587 (TLS) / Varsayılan: 587 (TLS)
SMTP_PORT=587

# SMTP authentication username
# SMTP kimlik doğrulama kullanıcı adı
# Usually your email address
# Genellikle e-posta adresiniz
SMTP_USER=

# SMTP authentication password
# SMTP kimlik doğrulama şifresi
# Usually your email password or app-specific password
# Genellikle e-posta şifreniz veya uygulamaya özel şifre
SMTP_PASSWORD=

# -----------------------------------------------------------------------------
# Object Storage (S3/MinIO) Configuration / Nesne Depolama (S3/MinIO) Yapılandırması
# -----------------------------------------------------------------------------

# S3-compatible storage endpoint URL
# S3 uyumlu depolama endpoint URL'si
# For local development with MinIO (Docker Compose):
# MinIO ile yerel geliştirme için (Docker Compose):
#   http://minio:9000
#
# For production AWS S3:
# Üretim AWS S3 için:
#   https://s3.amazonaws.com
#   or region-specific: https://s3.us-east-1.amazonaws.com
#   veya bölge özel: https://s3.us-east-1.amazonaws.com
#
# Optional: If not provided, file storage features will be limited
# Opsiyonel: Sağlanmazsa, dosya depolama özellikleri sınırlı olacaktır
S3_ENDPOINT=

# S3 access key ID
# S3 erişim anahtarı ID'si
# For local MinIO: minio (default from docker-compose.yml)
# Yerel MinIO için: minio (docker-compose.yml'den varsayılan)
# For production: Use IAM user access key
# Üretim için: IAM kullanıcı erişim anahtarı kullanın
S3_ACCESS_KEY_ID=

# S3 secret access key
# S3 gizli erişim anahtarı
# For local MinIO: miniosecret (default from docker-compose.yml)
# Yerel MinIO için: miniosecret (docker-compose.yml'den varsayılan)
# For production: Use IAM user secret key (keep secure!)
# Üretim için: IAM kullanıcı gizli anahtarı kullanın (güvende tutun!)
S3_SECRET_ACCESS_KEY=

# Default S3 bucket name for storing files
# Dosya depolama için varsayılan S3 bucket adı
# Bucket will be created automatically if it doesn't exist
# Bucket yoksa otomatik olarak oluşturulacaktır
S3_BUCKET=

# -----------------------------------------------------------------------------
# PAYTR Payment Gateway Configuration / PAYTR Ödeme Ağ Geçidi Yapılandırması
# -----------------------------------------------------------------------------

# PAYTR merchant ID
# PAYTR satıcı ID'si
# Get from PAYTR merchant panel
# PAYTR satıcı panelinden alın
PAYTR_MERCHANT_ID=

# PAYTR merchant key
# PAYTR satıcı anahtarı
# Get from PAYTR merchant panel
# PAYTR satıcı panelinden alın
PAYTR_MERCHANT_KEY=

# PAYTR merchant salt
# PAYTR satıcı salt'ı
# Get from PAYTR merchant panel
# PAYTR satıcı panelinden alın
PAYTR_MERCHANT_SALT=

# PAYTR API base URL
# PAYTR API temel URL'si
# Default: https://www.paytr.com
# Varsayılan: https://www.paytr.com
PAYTR_BASE_URL=https://www.paytr.com

# -----------------------------------------------------------------------------
# External LLM API Keys / Harici LLM API Anahtarları
# -----------------------------------------------------------------------------

# OpenAI API key
# OpenAI API anahtarı
# Optional: Only required if using OpenAI models
# Opsiyonel: Yalnızca OpenAI modelleri kullanılıyorsa gerekli
# Get from: https://platform.openai.com/api-keys
# Şuradan alın: https://platform.openai.com/api-keys
OPENAI_API_KEY=

# OpenAI API base URL
# OpenAI API temel URL'si
# Default: https://api.openai.com/v1
# Varsayılan: https://api.openai.com/v1
# Can be used for OpenAI-compatible APIs (e.g., local proxies)
# OpenAI uyumlu API'ler için kullanılabilir (örn. yerel proxy'ler)
OPENAI_BASE_URL=https://api.openai.com/v1

# Anthropic API key
# Anthropic API anahtarı
# Optional: Only required if using Claude models
# Opsiyonel: Yalnızca Claude modelleri kullanılıyorsa gerekli
# Get from: https://console.anthropic.com/
# Şuradan alın: https://console.anthropic.com/
ANTHROPIC_API_KEY=

# Embedding API key
# Embedding API anahtarı
# Used for generating embeddings for knowledge base
# Bilgi tabanı için embedding'ler oluşturmak için kullanılır
# Usually same as OPENAI_API_KEY if using OpenAI embeddings
# OpenAI embedding'leri kullanılıyorsa genellikle OPENAI_API_KEY ile aynı
EMBEDDING_API_KEY=

# Embedding model name
# Embedding model adı
# Default: text-embedding-3-small
# Varsayılan: text-embedding-3-small
EMBEDDING_MODEL=text-embedding-3-small

# -----------------------------------------------------------------------------
# Application URLs / Uygulama URL'leri
# -----------------------------------------------------------------------------

# Base URL for the application
# Uygulama için temel URL
# Used for generating links in emails and redirects
# E-postalardaki ve yönlendirmelerdeki bağlantıları oluşturmak için kullanılır
# For local development:
# Yerel geliştirme için:
#   http://localhost:3000
#
# For production:
# Üretim için:
#   https://yourdomain.com
APP_BASE_URL=http://localhost:3000

# Web frontend base URL
# Web frontend temel URL'si
# Used for SSO redirects
# SSO yönlendirmeleri için kullanılır
WEB_BASE_URL=http://localhost:5173

# Public API base URL (exposed to frontend)
# Genel API temel URL'si (frontend'e açık)
# Used by Next.js frontend to connect to API Gateway
# Next.js frontend'in API Gateway'e bağlanması için kullanılır
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000

# -----------------------------------------------------------------------------
# CORS Configuration / CORS Yapılandırması
# -----------------------------------------------------------------------------

# Comma-separated list of allowed origins for CORS
# CORS için izin verilen origin'lerin virgülle ayrılmış listesi
# For local development:
# Yerel geliştirme için:
#   http://localhost:3000,http://localhost:5173,http://localhost:3001
#
# For production:
# Üretim için:
#   https://yourdomain.com,https://app.yourdomain.com
#
# Leave empty or unset to allow all origins (NOT recommended for production)
# Tüm origin'lere izin vermek için boş bırakın veya ayarlamayın (üretim için ÖNERİLMEZ)
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173,http://localhost:3001

# -----------------------------------------------------------------------------
# Rate Limiting / Hız Sınırlama
# -----------------------------------------------------------------------------

# Maximum number of requests per time window
# Zaman penceresi başına maksimum istek sayısı
# Default: 100 / Varsayılan: 100
RATE_LIMIT_MAX=100

# Rate limit time window in milliseconds
# Hız sınırı zaman penceresi (milisaniye cinsinden)
# Default: 60000 (1 minute) / Varsayılan: 60000 (1 dakika)
RATE_LIMIT_TIME_WINDOW_MS=60000

# -----------------------------------------------------------------------------
# Worker Jobs Configuration / Worker İşleri Yapılandırması
# -----------------------------------------------------------------------------

# Worker concurrency (number of concurrent jobs)
# Worker eşzamanlılığı (eşzamanlı iş sayısı)
# Adjust based on system resources
# Sistem kaynaklarına göre ayarlayın
# Default: 5 / Varsayılan: 5
WORKER_CONCURRENCY=5

# Directory for exporting conversation data
# Konuşma verilerini dışa aktarma dizini
# Default: ./exports (relative to worker process)
# Varsayılan: ./exports (worker process'e göre)
EXPORT_DIR=./exports

# -----------------------------------------------------------------------------
# PM2 Process Manager Configuration / PM2 İşlem Yöneticisi Yapılandırması
# -----------------------------------------------------------------------------

# Number of PM2 instances to run
# Çalıştırılacak PM2 instance sayısı
# Use 'max' for all CPU cores, or specific number
# Tüm CPU çekirdekleri için 'max' veya belirli bir sayı kullanın
# Default: 1 / Varsayılan: 1
PM2_INSTANCES=1

# Maximum memory before PM2 restarts the process
# PM2'nin işlemi yeniden başlatmadan önce maksimum bellek
# Default: 1G / Varsayılan: 1G
PM2_MAX_MEMORY=1G

# PM2 error log file path
# PM2 hata günlük dosyası yolu
# Default: /var/log/pm2/api-gateway-error.log
# Varsayılan: /var/log/pm2/api-gateway-error.log
PM2_ERROR_LOG=/var/log/pm2/api-gateway-error.log

# PM2 output log file path
# PM2 çıktı günlük dosyası yolu
# Default: /var/log/pm2/api-gateway-out.log
# Varsayılan: /var/log/pm2/api-gateway-out.log
PM2_OUT_LOG=/var/log/pm2/api-gateway-out.log

# PM2 script path (auto-detected if not set)
# PM2 script yolu (ayarlanmazsa otomatik algılanır)
PM2_SCRIPT_PATH=

# PM2 Node.js arguments
# PM2 Node.js argümanları
# Default: --enable-source-maps
# Varsayılan: --enable-source-maps
PM2_NODE_ARGS=--enable-source-maps

# PM2 environment file path
# PM2 ortam dosyası yolu
# Default: .env
# Varsayılan: .env
PM2_ENV_FILE=.env

# -----------------------------------------------------------------------------
# Miscellaneous Settings / Çeşitli Ayarlar
# -----------------------------------------------------------------------------

# Default tenant/organization name
# Varsayılan kiracı/organizasyon adı
# Used when creating the default organization
# Varsayılan organizasyon oluşturulurken kullanılır
DEFAULT_TENANT_NAME=Default Org

# Allow user signup
# Kullanıcı kaydına izin ver
# Set to 'true' to allow public signup, 'false' to disable
# Genel kayda izin vermek için 'true', devre dışı bırakmak için 'false' olarak ayarlayın
ALLOW_SIGNUP=true
